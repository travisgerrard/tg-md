// Generated by CoffeeScript 1.8.0
var AutoupdateTime, React, Time, moment;

React = require('react');

Time = require('react-time');

AutoupdateTime = require('react-autoupdate-time');

moment = require('moment');

module.exports = React.createClass({
  displayName: 'SmartTimeAgo',
  getDefaultProps: function() {
    return {
      value: new Date()
    };
  },
  render: function() {
    if (2 >= moment().diff(moment(this.props.value), 'weeks')) {
      return React.createElement(AutoupdateTime, React.__spread({}, this.props));
    } else {
      return React.createElement(Time, React.__spread({
        "format": "D MMM YYYY"
      }, this.props));
    }
  }
});
